# Contributing

欢迎提交修复和改进。问题报告请包含操作系统、Node.js 版本、复现步骤和预期行为；请删去日志中的私人技能内容和本地路径。

1. Fork 仓库并创建功能分支。
2. `npm ci` 安装锁定依赖，`npm start` 启动本地应用。
3. 修改后运行 `npm run typecheck`、`npm test`、`npm run build`。
4. UI 改动请在目标浏览器检查选择联动、键盘操作、窄屏和减少动态效果模式；手势改动请使用触控板或触屏验证。
5. 提交 Pull Request，说明用户可见变化、验证结果与限制。

测试必须使用临时技能文件，不应依赖贡献者的真实技能库。新增文件操作请保持技能 ID 校验、同源限制和不经 Shell 拼接的调用方式。不要提交 `.env`、缓存、`node_modules` 或扫描得到的私人技能内容。
